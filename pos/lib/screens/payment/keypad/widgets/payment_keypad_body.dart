import 'package:flutter/material.dart';

class PaymentKeypadBody extends StatelessWidget {
  const PaymentKeypadBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PaymentKeypadBody'),
    );
  }
}
